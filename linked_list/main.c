#include <stdio.h>
#include "linkedList.h"

int addresses[1000];


int main() {

    size(100);
    createNode(100);
    createNode(200);
    createNode(300);
    createNode(400);
    createNode(500);
    printNodes();
    // removeNode(1);
    // printNodes();
}
